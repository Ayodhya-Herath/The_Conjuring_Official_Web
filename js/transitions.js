/* Cross-page view transitions — handled by CSS @view-transition { navigation: auto; }
   This file is kept as a no-op for browsers that don't support the CSS rule.
   No JS intervention needed — the browser handles MPA transitions natively. */
